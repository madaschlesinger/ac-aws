# {{ansible_managed}}

[Unit]
Description=ConsulAgent
After=docker.service
Wants=docker.service

[Service]
ExecStart=/usr/local/bin/consul agent -data-dir /var/consul -bind=MASTER -config-dir /etc/consul.d/server
Restart=always
RestartSec=20

[Install]
WantedBy=multi-user.target
